export interface UseOptions {
    directory: string;
    config?: string;
    reset?: boolean;
}
export default function use({ directory, config, reset }: UseOptions): Promise<void>;
interface SaveCurrentConfigOptions {
    configFileName: string;
    directory: string;
}
export declare function saveCurrentConfig({ configFileName, directory }: SaveCurrentConfigOptions): Promise<void>;
export {};
