import { Config } from '@oclif/core';
export interface LinkOptions {
    commandConfig: Config;
    directory: string;
    apiKey?: string;
    configName?: string;
}
export default function link(options: LinkOptions): Promise<void>;
